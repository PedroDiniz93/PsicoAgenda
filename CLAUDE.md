# Ruflo - Claude Code Configuration

This file is a lightweight compatibility layer.
The source of truth for this repository is `AGENTS.md`.

## Minimal Rules

- Follow `AGENTS.md` first.
- Do only what was asked.
- Prefer editing existing files over creating new ones.
- Read a file before editing it.
- Never commit secrets, credentials, or `.env` files.
- Keep validation and agent-routing guidance in `AGENTS.md`.

## Legacy Note

Older Claude-specific swarm, memory, and routing guidance used to live here. It has been consolidated into `AGENTS.md` to reduce duplicated context.
